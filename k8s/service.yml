apiVersion: v1
kind: Service
metadata:
  name: chihesd
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "chihesd"
    port: 8080
  selector:
    app: chihesd
