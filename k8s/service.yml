apiVersion: v1
kind: Service
metadata:
  name: lictaw
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "lictaw"
    port: 8080
  selector:
    app: lictaw
