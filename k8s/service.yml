apiVersion: v1
kind: Service
metadata:
  name: okiler
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "okiler"
    port: 8080
  selector:
    app: okiler
